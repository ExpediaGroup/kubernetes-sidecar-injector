# Default values for kube sidecar intector
# This is a YAML-formatted file.
# Declare name/value pairs to be passed into your templates.
# name: value

image:
  repository: expediagroup/kubernetes-sidecar-injector
  tag: latest